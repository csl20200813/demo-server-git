spring:
  profiles:
    active: dev
  datasource:
    url: 'jdbc:mysql://localhost:3306/demo-test?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&useSSL=true'
    username: 'root'
    password: 'root'
    driverClassName: 'com.mysql.cj.jdbc.Driver'

#  main:
#    allow-bean-definition-overriding: true
server:
  port: 8877
#mybatis-plus:
#  mapper-locations: classpath:/mapper/**/*.xml
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
myargs:
  thread:
    core-size: 20
    max-size: 200
    keep-alive-time: 10

